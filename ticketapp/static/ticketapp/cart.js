


document.addEventListener('DOMContentLoaded', function() {
    cartlist = document.querySelector("#cart");
    
    cartlist.innerHTML = ''
    //console.log(tickets);
    //tickets = JSON.stringify(tickets1.ticket)
    //console.log(typeof tickets1.ticket);
    //tickets = tickets.pop()
    //tickets = tickets.shift()
    //tickets = JSON.parse(tickets);
    //console.log(tickets[0]);
    /*
    for (let i = 0; i < tickets.length; i++) {
        formattedTicket = tickets[i];
        li = document.createElement("li");
        li.innerHTML = formattedTicket;
        cartlist.append(li);
    };*/
})